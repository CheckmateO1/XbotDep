name: XbotDep V1.1.3 Preflight

on:
  push:
    branches: [ main ]

jobs:
  static-preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/accept_v1_1.py
